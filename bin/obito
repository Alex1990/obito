#!/usr/bin/env node

const program = require('commander')

program
  .version(require('../package.json').version)
  .command('sync <package> [otherPackages...]', 'sync the package(s) to cloud storage')

program.parse(process.argv)
